- 아이돌 육성 시뮬레이션 게임
- 모바일 세로 해상도

### MVP1-1. 멤버카드
- 멤버 카드 좌측에 크게 얼굴 이미지
- 멤버 카드 우측에 이름과 상세 스펙
- 모든 타입들을 동그라미에 RGB 색상을 채운 식으로 표현한다.
- 멤버 카드 상세 스펙: 외모타입(RGB), 외모레벨(1~10), 보컬타입(RGB), 보컬레벨(1~10), 안무타입(RGB), 안무레벨(1~10)

### MVP1-2. 멤버카드 리스트
- 게임 화면 하단
- 멤버 카드 리스트는 왼쪽에서 오른쪽으로 멤버 카드들이 나열 되어 있으며 공간이 모자란 경우 스와이프를 지원한다

### MVP1-3. 파트 블록 리스트
- 게임 화면 가운데
- 왼쪽에서 오른쪽으로 파트 공간이 있으며 멤버카드를 드래그해 블록으로 복사할 수 있다.
- 파트블록은 테스트 버전으로 인트로, 파트1-1, 파트1-2, 싸비, 파트2-1, 파트-2-2, 싸비, 브릿지, 싸비 이렇게 리스팅한다.
- 멤버 카드를 삭제하고 싶으면 드래그할 때 최상단에 휴지통 아이콘으로 드랍하면 된다.

### MVP1-4. 파트 블록
- 파트 블록 상세 스펙
    - 포인트는 멤버카드와 파트 블록 스펙 간 계산 결과
    - 외모타입(RGB), 보컬타입(RGB), 안무타입(RGB)과의 상성이 있음
    - RGB중 하나는 파트 포인트를 350% 높여주며 또 다른 하나는 100%, 또 다른 하나는 15% 이다. *(채팅 피드백: 기존 200/100/50은 스탯 좋은 멤버가 모든 파트를 독식하는 문제가 있어 배율 간격 확대)*

### MVP1-5. 곡 포인트
- 파트 블록과의 상성 계산 식이 나타남
- 모든 블록에 멤버카드를 배치하고 각 파트블록 포인트의 합산이 곡의 포인트가 된다.
- 화면 상단 헤더에 곡 포인트 종합 점수 합산 결과가 나온다.

### MVP1-6. 곡 정보 · 확정
- 헤더 왼쪽 하단에 곡제목 표시 (예: `곡제목: Fly(가제)`)
- 모든 파트에 멤버가 배치되면 곡제목 오른쪽 **확정** 버튼 활성화
- 확정 후 멤버 배치가 바뀌면 확정 상태 초기화

---

### 채팅에서 도출된 이슈 · 개선

#### RGB 상성 밸런스
- **이슈**: 상성 배율(200/100/50)이 약해 스탯이 높은 멤버가 어느 파트든 유리함 → 파트별 배치 전략이 의미 없음
- **조치**: 강 350% / 중 100% / 약 15%로 조정 *(구현됨)*

#### 멤버카드 상성 가독성
- **이슈**: 드래그·배치 시 어떤 스탯이 이 파트에 맞는지 한눈에 안 보임
- **조치**: 파트 컨텍스트가 있을 때 강 상성 스탯 배경 강조 + 약 상성 흐림, 드래그 고스트·파트 블록 스탯 행에 동일 적용 *(구현됨)*

#### UI 피드백 (왼쪽 굵은 선)
- **이슈**: 강 상성 강조용 왼쪽 세로선(`box-shadow` 악센트)이 무엇인지 불명확
- **조치**: 배경 하이라이트와 중복이라 제거 *(구현됨)*

#### 배치 쾌감 (juice)
- **이슈**: 드래그·드롭·점수 획득 피드백이 밋밋함
- **조치** *(구현됨)*
    - 드래그 중 파트 호버: 블록 확대·글로우, 상성 1개/2개 이상일 때 단계별 강조
    - 드롭 순간: 블록 팝 애니메이션, `+포인트` 플로팅, 스탯 순차 등장
    - 강 상성 2개 이상 드롭: 초록 글로우 + 모바일 진동(지원 기기)
    - 곡 포인트 헤더: 점수 변경 시 펀치 애니메이션

#### 곡 확정 UX
- **이슈**: 파트를 다 채운 뒤 다음 액션(확정)이 없음
- **조치**: 곡제목 + 확정 버튼 추가, 전 파트 배치 완료 시에만 활성화 *(구현됨)*

#### 미결 / 후속 검토
- **확정 버튼 클릭 후 플로우**: 확정 이후 화면 전환·저장·결과 표시 등 후속 UX 미정의
- **상성 배율 추가 튜닝**: 350/100/15 체감이 과하거나 부족할 경우 재조정 필요 여부 플레이테스트
- **하단 멤버 목록**: 파트 컨텍스트 없이는 상성 강조 미표시 — 목록에서도 미리보기가 필요한지 검토
